{
    "name": "warp-style-extension",
    "displayName": "Warp‑Style Extension",
    "description": "VSCode extension with AI agents, terminal panel, and context management.",
    "version": "0.0.1",
    "engines": {
        "vscode": "^1.90.0"
    },
    "main": "./out/extension.js",
    "scripts": {
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "pretest": "npm run compile",
        "test": "node ./out/test/runTest.js"
    },
    "devDependencies": {
        "@types/vscode": "^1.90.0",
        "typescript": "^5.5.0",
        "vscode-test": "^1.9.0"
    },
    "dependencies": {
        "vscode-languageclient": "^9.0.0"
    }
}