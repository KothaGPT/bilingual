# শুরু করুন

# 🚀 **বাইলিঙ্গুয়াল দিয়ে শুরু করুন**

এই গাইডটি আপনাকে বাইলিঙ্গুয়াল এনএলপি টুলকিট দিয়ে কয়েক মিনিটের মধ্যে শুরু করতে সাহায্য করবে।

## প্রয়োজনীয়তাসমূহ

- Python 3.8 বা উচ্চতর
- pip প্যাকেজ ম্যানেজার
- (ঐচ্ছিক) দ্রুততর মডেল ইনফারেন্সের জন্য CUDA-সামঞ্জস্যপূর্ণ GPU

## ইনস্টলেশন

pip ব্যবহার করে প্যাকেজটি ইনস্টল করুন:

```bash
pip install bilingual
```

সমস্ত ঐচ্ছিক নির্ভরতাসহ ডেভেলপমেন্ট ইনস্টলেশনের জন্য:

```bash
pip install bilingual[dev]
```

বা সোর্স থেকে ইনস্টল করুন:

```bash
git clone https://github.com/bilingual-nlp/bilingual.git
cd bilingual
pip install -e .
```

## দ্রুত শুরু

### 1. বেসিক টেক্সট প্রসেসিং

```python
import bilingual as bb

# ভাষা শনাক্তকরণ
text = "আমি স্কুলে যাই এবং বই পড়তে ভালোবাসি।"
result = bb.detect_language(text)
print(f"ভাষা: {result['language']}")  # আউটপুট: bengali

# একাধিক টাস্ক সহ টেক্সট প্রসেস করুন
processed = bb.process(text, tasks=["normalize", "tokenize"])
print(processed)
```

### 2. অনুবাদ

```python
# ভাষার মধ্যে অনুবাদ করুন
english_text = "I love reading books"
bengali_translation = bb.translate_text(
    "t5-small", english_text, "en", "bn"
)

print(f"ইংরেজি: {english_text}")
print(f"বাংলা: {bengali_translation}")
```

### 3. টেক্সট জেনারেশন

```python
# ট্রান্সফর্মার মডেলসমূহ ব্যবহার করে টেক্সট জেনারেট করুন
prompt = "Write a short story about a brave child"
story = bb.generate_text("t5-small", prompt, max_length=100)

print(f"জেনারেটেড গল্প: {story}")
```

### 4. মূল্যায়ন

```python
# অনুবাদ কোয়ালিটি মূল্যায়ন করুন
references = ["I love reading books"]
candidates = ["I adore reading books"]

results = bb.evaluate_translation(references, candidates)
print(f"BLEU স্কোর: {results['bleu']:.4f}")
```

## কমান্ড লাইন ইন্টারফেস

টুলকিটটি একটি শক্তিশালী CLIও প্রদান করে:

```bash
# তথ্য দেখান
bilingual info

# টেক্সট প্রসেস করুন
bilingual process "Hello world" --tasks detect normalize tokenize

# টেক্সট অনুবাদ করুন
bilingual translate "Hello world" --from en --to bn

# অনুবাদসমূহ মূল্যায়ন করুন
bilingual evaluate translation --reference refs.txt --candidate candidates.txt

# টেক্সট জেনারেট করুন
bilingual generate "Write a story" --model t5-small
```

## কনফিগারেশন

কনফিগারেশন ব্যবহার করে টুলকিটের আচরণ কাস্টমাইজ করুন:

```python
from bilingual.config import get_settings

# বর্তমান সেটিংসমূহ পান
settings = get_settings()

# সেটিংসমূহ কাস্টমাইজ করুন
settings.model.default_model = "t5-base"
settings.evaluation.bleu_ngram_order = 4
```

এনভায়রনমেন্ট ভেরিয়েবলসমূহও ব্যবহার করা যায়:

```bash
export BILINGUAL_MODEL_DEFAULT_MODEL="t5-base"
export BILINGUAL_EVAL_BLEU_NGRAM_ORDER=4
```

## পরবর্তী ধাপসমূহ

- বিস্তারিত ফাংশন ডকুমেন্টেশনের জন্য [API রেফারেন্স](api/index.md) অন্বেষণ করুন
- আরও উন্নত ব্যবহারের ক্ষেত্রে [উদাহরণসমূহ](examples/index.md) দেখুন
- টুলকিট উন্নত করতে সাহায্য করার জন্য [কনট্রিবিউটিং](contributing/index.md) সম্পর্কে জানুন

## সমস্যা সমাধান

### সাধারণ সমস্যাসমূহ

**ইমপোর্ট এরর**: নিশ্চিত করুন যে সমস্ত নির্ভরতা ইনস্টল করা আছে:
```bash
pip install transformers torch sentencepiece
```

**CUDA সমস্যাসমূহ**: GPU সমর্থনের জন্য, PyTorch কে CUDA সহ ইনস্টল করুন:
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

**মেমরি সমস্যাসমূহ**: বড় মডেলসমূহের জন্য, ছোট ভ্যারিয়েন্টসমূহ ব্যবহার করুন:
```python
bb.load_model("t5-small")  # t5-base এর পরিবর্তে
```

### সাহায্য পাওয়া

- পরিচিত সমস্যাগুলির জন্য [GitHub Issues](https://github.com/bilingual-nlp/bilingual/issues) চেক করুন
- কমিউনিটি সমর্থনের জন্য আমাদের [GitHub Discussions](https://github.com/bilingual-nlp/bilingual/discussions) এ যোগ দিন
- GitHub-এ বাগ রিপোর্ট বা ফিচার রিকোয়েস্ট ফাইল করুন

---

*আরও অন্বেষণ করতে প্রস্তুত? [API রেফারেন্স](api/index.md) এ যান বা কিছু [উদাহরণ](examples/index.md) দেখুন!* 🚀
